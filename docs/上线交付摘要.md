# 交付摘要：HR 动态表单引擎 · 已上线

> 上线时间：2026-09-04 ｜ 部署目标：火山引擎 ECS ｜ 模式：演示（JSON 文件持久化）

## 线上地址（公网可直接访问）

- 服务入口：**http://115.190.238.188/**
- 健康检查：http://115.190.238.188/health → `200 {"ok":true}`
- Schema 接口：`GET /api/v1/forms/tech_onboarding/schema`（需令牌）

## 访问令牌

| 角色 | 令牌 | 用途 |
|---|---|---|
| admin | `R3iktPIG0y8qcyOz6a-g7ysAb1nkv9kK` | 管理端 |
| hr | `9W2Svl6BhVLvFgwQ9MwxWmp5CosdpCFc` | 表单录入 / 提交数据 / 上传附件 |
| viewer | `C8w0QrczE0cfDaGHlt_j9bLo7WSyICdv` | 只读：查 Schema / 分页查询 |

调用示例：
```
GET /health
GET /api/v1/forms/tech_onboarding/schema
    Authorization: Bearer C8w0QrczE0cfDaGHlt_j9bLo7WSyICdv
POST /api/v1/dynamic-data/tech_onboarding
    Authorization: Bearer 9W2Svl6BhVLvFgwQ9MwxWmp5CosdpCFc
```

## 服务器环境

- 系统：Ubuntu 24.04 LTS（4 vCPU / 8 GiB / 40G SSD）
- 运行时：Node.js v22.23.2 + pnpm 11.25.0
- 反向代理：nginx 1.24.0（80 → 127.0.0.1:3000）
- 服务：systemd `hr-form`（开机自启）｜ 代码 `/opt/hr-form` ｜ 配置 `.env.demo`
- 数据：`/opt/hr-form/data/state.json`（重启不丢数据）
- 附件：`/opt/hr-form/data/uploads/`

## 验收结果（公网实测）

1. 服务内部冒烟脚本 **7/7 通过**
2. 公网端到端全链路通过：
   - 健康检查 200
   - viewer 拉 Schema（v4 / 8 字段 / ETag）
   - hr 上传附件（init → PUT content，sha256 校验、文件落盘）
   - 提交合法表单（附件引用 + 条件必填 + 工作履历 repeater）→ 返回 `submissionId`
   - viewer 分页查询 `total=1`；审计日志正常记录

## 待办（正式对外前）

- [ ] ICP 备案后接入域名 + HTTPS（80/443 国内需备案）
- [ ] 安全组收敛（22 仅办公网开放）
- [ ] 演示模式切 MySQL + 对象存储生产模式
- [ ] 上传代码仓库（公钥已配，随时可推）
