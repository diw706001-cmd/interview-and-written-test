全栈开发⼯程师：HR 动态表单与通⽤接⼝引擎
限定条件： 允许使⽤ TypeScript/Node.js、Python、Go、PHP、Rust 等任意主流语⾔/框
架，严禁使⽤ Java。前端技术栈建议使⽤ React、Vue 3 或 Svelte。这⾥所有技术栈都是
建议，并不是范围限制（但是依然禁⽤ Java），您可以尽情发挥。
提交要求：请将最终成品部署⾄云端。 为⽅便您进⾏部署测试，请将您的 SSH 公钥提供给
对接⼈员，我们将为您统⼀开通 AWS 云服务器实例并提供访问权限。最后请将 repo 地址
和在线系统访问地址⼀并提交。
公司 HR 系统需要⽀持不同部⻔、不同岗位的个性化⼊职信息收集（如技术岗需填 GitHub 链
接，销售岗需填驾驶证信息）。HR 团队希望⽆需开发⼈员介⼊，即可通过后台配置出全新的
⼊职表单，并⾃动⽣成对应的 API 接⼝进⾏数据的提交、查询与校验。
场景⼀：动态表单 Schema 设计与前端渲染引擎
背景： HR 需要定义⼀个“技术岗位⼊职表单”，⽤于记录存档⼊职⼈员信息，字段类型包含⽂
本输⼊、下拉选择、⽂件上传以及嵌套数组（如“⼯作履历”）。
Q0：Schema 数据结构设计
请设计⼀套 JSON Schema，⽤于描述表单的字段结构，需包含：
同时⽀持：
Q1：前端动态渲染引擎
写出⼀个前端组件，接收上述 Schema，⾃动渲染对应的表单控件并管理表单状态
（ formState ）。
字段标识 ( key )、标签 ( label )、组件类型 ( type : input , select , upload ,
repeater )。
基础校验规则（必填、正则、最⼤/最⼩值、regex、email）。
嵌套 Object
嵌套 Array
字段默认值
Schema Version
字段废弃但历史数据仍存在的情况
 Schema 结构参考示例 (需由你在代码中进一步完善)
{
"formId": "tech_onboarding",
"title": "技术岗入职信息表",
"fields": [  ]
1
2
3
4
5

-- 1 of 3 --

重点⽀持：
进阶要求：考虑以下情况并防⽌ Rule Engine ⽆限循环：
Q2：Schema ⽣命周期与版本管理
某个表单当前 Schema Version 为 v3，已有 10,000 条历史提交记录。HR 修改了表单：
employeeName 改名为 fullName；删除 nickname；新增必填字段 department。请设计
Schema 的⽣命周期，要求：
场景⼆：可配置通⽤数据 API 引擎
背景： 系统不能为每⼀个新创建的表单单独编写路由代码，必须提供⼀套通⽤的 CRUD 接⼝
引擎。
Q0：通⽤提交接⼝
POST /api/v1/dynamic-data/:formId
Q1：通⽤查询与筛选接⼝
}	6
动态添加/删除数据项
Rule Engine
字段及错误提示
表单级错误提示
Dirty State
Loading State
Server Validation Error
A 修改 B
B 修改 C
C 又修改 A
1
2
3
已发布 Schema 不允许直接修改
修改后⽣成新的 Schema Version
历史 Submission 必须保留原 Schema Version
新 Submission 使⽤最新 Published Version
接⼝接收 :formId 及 payload 数据。
⾃动解析 Schema，校验提交的 JSON 数据是否合法。若校验失败返回详细的错误提示
（如： 字段 email 格式不正确 ）。
校验通过后，将数据持久化存储（数据库设计不限：如 PostgreSQL JSONB、MongoDB
或 SQLite JSON 等）。

-- 2 of 3 --

GET /api/v1/dynamic-data/:formId
Q2：字段联动与⾼级表达式引擎
HR 提出需求：表单字段之间存在联动逻辑。例如：
Q3：⽂件上传安全
动态表单需要⽀持身份证、合同、简历、银⾏卡资料等敏感⽂件上传。请设计⽂件上传流程。
禁⽌直接将⼤⽂件 Binary 保存到 Submission JSON。
Q4：⽣命周期钩⼦与扩展机制
为了满⾜ HR 系统未来的扩展需求（如：提交表单后触发钉钉通知、⾃动⽣成 PDF 合同、将
候选⼈同步⾄第三⽅考勤系统），通⽤接⼝必须⽀持插件/钩⼦机制。
Q5：并发修改
两个 HR 同时打开 Form Builder，两⼈读取 schemaVersion = 10。HR A 修改并保存，⽣成
version = 11；与此同时 HR B 也提交⾃⼰的修改。请设计解决⽅案，避免 B 覆盖 A 的修改。
可以考虑 Optimistic Locking、ETag、version field。
Q6：审计⽇志
系统涉及 HR 敏感数据，需要知道谁在什么时候修改了什么。例如 salary：30000 -> 35000。
⽀持通过 JSON 字段进⾏分⻚与动态筛选（如：查询所有 experience > 3 的员⼯）。
当“是否应届⽣”选择 是 时，隐藏“前公司名称”字段，并取消其“必填”校验。
当“期望薪资”⼤于 30000 时，⾃动显示“需要 HRBP 特别审批”提示⽂本。

-- 3 of 3 --

